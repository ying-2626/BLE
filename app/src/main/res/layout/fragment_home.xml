<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="70dp"
        android:background="@color/purple_500"
        android:elevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="我的个人主页"
            android:textColor="@android:color/white"
            android:textSize="22sp"
            android:gravity="center"
            android:layout_marginTop="30dp"/>
    </androidx.appcompat.widget.Toolbar>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="40dp">
    <!-- 头像区域 -->
    <ImageView
        android:id="@+id/avatar_image"
        android:layout_marginTop="100dp"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/default_avatar"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 用户信息区域 -->
    <TextView
        android:id="@+id/username_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="用户名："
        android:textSize="20sp"
        app:layout_constraintTop_toBottomOf="@id/avatar_image"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="24dp"/>

    <TextView
        android:id="@+id/username_value_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/deep_grey"
        android:textSize="20sp"
        app:layout_constraintStart_toEndOf="@id/username_text"
        app:layout_constraintTop_toTopOf="@id/username_text"/>

    <TextView
        android:id="@+id/contact_info_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="联系方式："
        android:textSize="20sp"
        app:layout_constraintTop_toBottomOf="@id/username_text"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="25dp"/>

    <TextView
        android:id="@+id/contact_info_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/deep_grey"
        android:textSize="20sp"
        app:layout_constraintStart_toEndOf="@id/contact_info_text"
        app:layout_constraintTop_toTopOf="@id/contact_info_text"/>

    <!-- 操作按钮区域 -->
    <Button
        android:id="@+id/modify_info_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="修改信息"
        android:textSize="17dp"
        app:layout_constraintTop_toBottomOf="@id/contact_info_text_view"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/modify_password_button"
        android:layout_marginTop="32dp"/>

    <Button
        android:id="@+id/modify_password_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="修改密码"
        android:textSize="17dp"
        android:layout_marginTop="5dp"
        app:layout_constraintTop_toBottomOf="@id/modify_info_button"
        app:layout_constraintStart_toEndOf="@id/modify_info_button"
        app:layout_constraintEnd_toEndOf="parent"/>

        <Button
            android:id="@+id/exit_login_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="退出登录"
            android:textSize="17dp"
            android:layout_marginTop="50dp"
            app:layout_constraintTop_toBottomOf="@id/modify_password_button"
            app:layout_constraintStart_toEndOf="@id/modify_password_button"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>